[pytest]
; Force serial execution - run one test at a time
; This prevents rate limiting issues when hitting external APIs
addopts = -p no:xdist
markers =
    serial: mark test to run serially
    integration: mark a test that talks to an external service
    live: mark an opt-in live website test (run with --run-live)
